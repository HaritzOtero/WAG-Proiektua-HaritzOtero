import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NireErreserbakPage } from './nire-erreserbak.page';

describe('NireErreserbakPage', () => {
  let component: NireErreserbakPage;
  let fixture: ComponentFixture<NireErreserbakPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NireErreserbakPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
