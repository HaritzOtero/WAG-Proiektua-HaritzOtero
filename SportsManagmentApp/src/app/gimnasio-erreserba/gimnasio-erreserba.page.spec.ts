import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GimnasioErreserbaPage } from './gimnasio-erreserba.page';

describe('GimnasioErreserbaPage', () => {
  let component: GimnasioErreserbaPage;
  let fixture: ComponentFixture<GimnasioErreserbaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GimnasioErreserbaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
