import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NireErreserbakGymPage } from './nire-erreserbak-gym.page';

describe('NireErreserbakGymPage', () => {
  let component: NireErreserbakGymPage;
  let fixture: ComponentFixture<NireErreserbakGymPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NireErreserbakGymPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
