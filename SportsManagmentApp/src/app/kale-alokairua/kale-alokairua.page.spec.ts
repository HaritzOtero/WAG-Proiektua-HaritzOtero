import { ComponentFixture, TestBed } from '@angular/core/testing';
import { KaleAlokairuaPage } from './kale-alokairua.page';

describe('KaleAlokairuaPage', () => {
  let component: KaleAlokairuaPage;
  let fixture: ComponentFixture<KaleAlokairuaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(KaleAlokairuaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
